click>=8.1.7
pydantic>=2.8.2
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Optional local audio backends. Install only when you are ready for GPU/audio work:
# pip install faster-whisper demucs soundfile
